#include <main.h>
/**
 * main - print alphabet in lowercase
 *
 * Return: return(0)
 */
int main(void)
{
	int (letter);

	for (letter = a, > z, letter++);
		putchar(letter);
	{
		if (letter = a, b, c, d);
	}
	putchar ('\n')
	return (0)
}
