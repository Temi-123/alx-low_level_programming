#include "main.h"
/**
 * main - check if argument is alpha
 *
 * Return: Return(0)
 */
int main(void)
	int (n)
{
	int_isalpha(int c)
	{
		if (c >= 97 && c <= 122)
			return (1);
		else
			return (0);
	}
'}'
