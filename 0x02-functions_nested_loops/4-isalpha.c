#include "main.h"
/**
 * -isalpha: check if arguement with character
 * @c: the character to be checked
 *
 * Return: Return(0)
 */

int _isalpha(int c)
{
		if (c >= 97 && c <= 122)
	{
			return (1);
	}
	
			return (0);
}
